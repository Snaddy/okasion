<%= form_for @user, url: add_city_path do |f| %>

<div class="field">
    <%= f.label :city %><br />
    <%= f.text_field :city, autofocus: true %>
</div>

<div class="actions">
    <%= f.submit "Update" %>
</div>

<% end %>